[build-system]
requires = ["setuptools==65.5.1", "wheel==0.38.0", "pyproject-hooks==1.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "foodscanner"
version = "0.1.0"
description = "Food Scanner Application"
requires-python = ">=3.8"